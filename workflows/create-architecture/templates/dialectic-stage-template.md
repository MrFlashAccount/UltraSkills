# Create-architecture dialectic stage

Return the schema-defined structured output and write any required artifact inside this step's artifact output directory.

This is a non-implementation phase. Inspect evidence read-only; do not run tests, lint, validation, typecheck, build checks, `git diff --check`, or equivalent verification commands.

- Preserve accepted artifact metadata, evidence, and lineage exactly.
- Compare architecture decisions, not prose similarity.
- Do not write canonical repository architecture artifacts before approval.
- Do not exchange hidden reasoning, transcripts, or peer synthesis output.
