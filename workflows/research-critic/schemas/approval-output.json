{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Research Critic Approval Output",
  "type": "object",
  "required": [
    "approval"
  ],
  "properties": {
    "approval": {
      "enum": [
        "approved"
      ],
      "description": "Human answer gate decision for the research questions step.",
      "x-usage": "Use approved when user answers are ready for research_answered_draft, Report a non-blocking stop through the runner control channel when research needs external help."
    },
    "answers": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Optional structured user answers captured by the approval gate.",
      "x-usage": "Use as the authoritative user answers when present."
    },
    "results": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/result"
      },
      "description": "Optional compact summary of approved user answers.",
      "x-usage": "Use as supporting answer context; do not route from this field."
    }
  },
  "additionalProperties": true
}
