{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DevHarness Approval Output",
  "type": "object",
  "required": [
    "approval"
  ],
  "properties": {
    "approval": {
      "enum": [
        "approved",
        "rejected"
      ],
      "description": "Human gate decision for the current DevHarness approval step.",
      "x-usage": "Use approved to continue, rejected to revise the gated draft step, Report a non-blocking stop through the runner control channel when the request needs external help."
    },
    "results": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/result"
      },
      "description": "Optional compact summary of the human approval decision or feedback.",
      "x-usage": "Use as supporting human gate context; do not route from this field."
    },
    "artifacts": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/artifact"
      },
      "description": "Optional artifacts attached by the approval gate.",
      "x-usage": "Use only when the approval gate explicitly attaches artifacts."
    }
  },
  "additionalProperties": true
}
