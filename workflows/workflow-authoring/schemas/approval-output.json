{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Workflow Authoring Approval Output",
  "type": "object",
  "required": [
    "approval"
  ],
  "properties": {
    "approval": {
      "enum": [
        "approved",
        "rejected"
      ],
      "description": "Human workflow-design gate decision.",
      "x-usage": "Use approved to implement, rejected to revise workflow_design_draft, Report a non-blocking stop through the runner control channel when authoring needs external help."
    },
    "results": {
      "type": "array",
      "items": {
        "$ref": "https://github.com/MrFlashAccount/Skills/schemas/workflow/baton#/$defs/result"
      },
      "description": "Optional compact summary of human approval feedback.",
      "x-usage": "Use as supporting approval context; do not route from this field."
    }
  },
  "additionalProperties": true
}
